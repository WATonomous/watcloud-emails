{
  "name": "@watonomous/watcloud-mailer",
  "description": "A server to send emails",
  "devDependencies": {
    "@repo/ts-config": "*",
    "@types/express": "^5.0.3",
    "@watonomous/watcloud-email-templates": "*",
    "concurrently": "^9.2.0",
    "tsup": "^8.5.0",
    "typescript": "^5.6.3"
  },
  "scripts": {
    "dev": "concurrently \"tsup --watch\" \"docker compose up --build --watch\"",
    "build": "tsup",
    "check-types": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
